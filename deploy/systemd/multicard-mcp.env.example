# Directory with markdown docs bundled in this repo
MULTICARD_DOCS_DIR=/opt/multicard-mcp/multicard-docs

# Keep it on localhost and reverse-proxy with nginx/caddy if needed
LISTEN_ADDR=127.0.0.1:8080
